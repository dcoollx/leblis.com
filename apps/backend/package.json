{
  "name": "backend",
  "version": "1.0.0",
  "description": "AWS Lambda functions",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "prebuild": "tsc --noEmit",
    "build": "rm -rf dist && esbuild src/index.ts --bundle --external:@aws-sdk/* --external:/opt/nodejs/* --platform=node --format=esm --target=node22 --outfile=dist/index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/lib-dynamodb": "^3.1030.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.19.39",
    "esbuild": "^0.25.12",
    "stripe": "^22.0.2",
    "typescript": "^5.3.3"
  }
}
